package com.green.hoteldog.common;

public class ABTestExample {

    /*public static void main(String[] args) {
        // Optimizely SDK를 초기화합니다.
        Optimizely optimizely = null;
        try {
            optimizely = Optimizely.builder()
                    .withSdkKey("your_sdk_key") // Optimizely 대시보드에서 발급받은 SDK 키를 입력하세요.
                    .build();
        } catch (ConfigParseException e) {
            e.printStackTrace();
        }

        // Optimizely SDK로부터 ProjectConfig를 가져옵니다.
        ProjectConfig projectConfig = optimizely.getProjectConfig();

        // 테스트 그룹에 할당되었는지 확인합니다.
        String userId = "user123"; // 사용자 ID
        if (optimizely.isFeatureEnabled("your_experiment_key", userId)) {
            // 테스트 그룹에 할당된 경우 실행할 코드
            System.out.println("User " + userId + " is in the test group.");
        } else {
            // 제어 그룹에 할당된 경우 실행할 코드
            System.out.println("User " + userId + " is in the control group.");
        }

        // Optimizely SDK를 종료합니다.
        optimizely.close();
    }*/
}